services:
  app:
    x-cnc:
      type: serverless
       true
    build:
      context: .

  db1:
    x-cnc:
      type: database
      version: 15
      use_db_proxy: False
       True
    image: postgres
services:
  app:
    x-cnc:
      type: serverless
       true
    build:
      context: .


  dynamodb1:
    x-cnc:
      type: dynamodb


  dynamodb2:
    x-cnc:
      type: dynamodb

services:
  app:
    x-cnc:
      type: backend
    build:
      context: .

  db1:
    x-cnc:
      type: database
      version: 15
    image: postgres

  lambda1:
    x-cnc:
      type: serverless
    build:
      context: .

  dynamodb1:
    x-cnc:
      type: dynamodb
      
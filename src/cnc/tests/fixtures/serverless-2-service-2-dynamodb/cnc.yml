services:
  app:
    x-cnc:
      type: serverless
      lite: true
    build:
      context: .

  app2:
    x-cnc:
      type: serverless
      lite: true
    build:
      context: .

  dynamodb1:
    x-cnc:
      type: dynamodb


  dynamodb2:
    x-cnc:
      type: dynamodb

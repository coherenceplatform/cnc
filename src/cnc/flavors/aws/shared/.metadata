provider_links_templates:
    backend: "https://{{ service.environment.collection.region }}.console.aws.amazon.com/ecs/v2/clusters/{{ service.environment.collection.instance_name }}-cluster/services/{{ service.instance_name }}/deployments?region={{ service.environment.collection.region }}"
    worker: "https://{{ service.environment.collection.region }}.console.aws.amazon.com/ecs/v2/clusters/{{ service.environment.collection.instance_name }}-cluster/services/{{ service.instance_name }}_{{ worker.name }}/deployments?region={{ service.environment.collection.region }}"
    task: "https://{{ service.environment.collection.region }}.console.aws.amazon.com/ecs/v2/clusters/{{ service.environment.collection.instance_name }}-cluster/services/{{ service.instance_name }}_{{ task.name }}/deployments?region={{ service.environment.collection.region }}"
    database: "https://{{ service.environment.collection.region }}.console.aws.amazon.com/rds/home?region={{ service.environment.collection.region }}#database:id={{ service.instance_name }}"
    cache: "https://{{ service.environment.collection.region }}.console.aws.amazon.com/elasticache/home?region={{ service.environment.collection.region }}#/redis/{{ service.instance_name}}"
    frontend: "https://{{ service.environment.collection.region }}.console.aws.amazon.com/cloudfront/v3/home?region={{ service.environment.collection.region }}#/distributions"